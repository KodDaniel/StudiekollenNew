@model StudiekollenNew.ViewModels.SearchTestViewModel

<h2>Sök efter prov</h2>

@using (Html.BeginForm("SearchForTest", "Test"))
{
    <div class="form-horizontal">
        <hr />
        <div class="form-group">
            @Html.Label("Mina prov", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @*@Html.DropDownListFor(model => model.Username, new SelectList(Model.Users, "Username", "UserName", "Välj användare"), "Välj användare", new { @class = "form-control" })*@
                @Html.DropDownListFor(model => model.Id, new SelectList(Model.AllTests, "Id", "Name", null), null, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.AntiForgeryToken()
                <input type="submit" value="Hämta" class="btn btn-default btn-block" /><br />
            </div>
        </div>
        <h4>@Html.ActionLink("Visa alla prov", "DetailsTest", "Test", null)</h4> 
    </div>
}


