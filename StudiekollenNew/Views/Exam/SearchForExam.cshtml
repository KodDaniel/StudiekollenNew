@model StudiekollenNew.ViewModels.ExamViewModels.SearchExamViewModel
@section metatags
{
    <meta name="Title" content="@ViewBag.Title" />
    <meta name="Description" content="@ViewBag.Description" />
    <meta name="Keywords" content="@ViewBag.KeyWords" />
}

<h2>Sök efter prov</h2>

@using (Html.BeginForm("SearchForExam", "Exam"))
{
    <div class="form-horizontal">
        <hr />
        <div class="form-group">
            <label  class="control-label col-md-2">Mina prov</label>
            <div class="col-md-10">
                @*@Html.DropDownListFor(model => model.Username, new SelectList(Model.Users, "Username", "UserExamExamExamName", "Välj användare"), "Välj användare", new { @class = "form-control" })*@
                @Html.DropDownListFor(model => model.Id, new SelectList(Model.AllExams, "ExamId", "ExamName", null), null, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.AntiForgeryToken()
                <input type="submit" value="Hämta" class="btn btn-default btn-block" /><br />
            </div>
        </div>
        <h4>@Html.ActionLink("Visa alla prov", "DisplayExams", "Test", null)</h4>
    </div>
}


